import { Canvas, Meta } from '@storybook/addon-docs/blocks'

<Meta title="Локальная разработка/Миксины" />

# Миксины:
- для высот с `dvh` используйте `@inclide dvh(100)`.
- для кастомизации скроллбарров `@inclide scrollbar()`
- для кастомных шрифтов `@inclide font-size(h1)`