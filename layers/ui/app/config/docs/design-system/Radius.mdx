import { Canvas, Meta } from '@storybook/addon-docs/blocks'
import * as RadiusStories from './Radius.stories'

<Meta title="Система дизайна/Радиусы" />

# Скругления
- Варианты скругления задаются в `config/styles/variables/radius.scss` и зависят от заданного глобального класса `radius-*`. По умолчанию задаётся `radius-3`.
- Ниже можно посмотреть как меняются переменные в зависимости от этого класса.

<Canvas of={RadiusStories.RadiusTokens} />
