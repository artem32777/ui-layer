import { Canvas, Meta } from '@storybook/addon-docs/blocks'
import * as RadiusStories from './Radius.stories'

<Meta title="Система дизайна/Скругления" />

# Скругления
- Варианты скругления задаются в `config/styles/variables/radius.scss` и  зависят от значально заданного глобального класса `radius-*`. По умолчанию задаётся `radius-3`.
- Ниже можно посмотреть как.

<Canvas of={RadiusStories.RadiusTokens} />
