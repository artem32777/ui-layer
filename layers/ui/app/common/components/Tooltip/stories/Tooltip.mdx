import { Canvas, Controls, Meta, Source } from '@storybook/addon-docs/blocks'
import * as TooltipStories from './Tooltip.stories'
import TooltipExampleSource from './TooltipExample.vue?raw'

<Meta of={TooltipStories} />

# Tooltip
- Основан на <font color="blue">[`Tooltip из Reka UI`](https://reka-ui.com/docs/components/tooltip)</font>;
- Использует либо пропс `text` для строковых подсказок, либо слот `content` для ссылок, иконок и пр;
- Для более сложных кейсов сменатически правильнее использовать компонент `Popover`;

<Canvas of={TooltipStories.Base} />

<Controls of={TooltipStories.Base} />

## Пример использования

<Source code={TooltipExampleSource} language="vue" />
