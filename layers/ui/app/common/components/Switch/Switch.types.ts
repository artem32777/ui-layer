export interface SwitchProps {
	/** Текст справа от чекбокса, если не используется слот */
	text?: string
	/** Отключает переключатель и запрещает взаимодействие. */
	disabled?: boolean
	/** Отмечает переключатель как невалидный. */
	invalid?: boolean
}
