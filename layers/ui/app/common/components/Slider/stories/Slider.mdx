import { Canvas, Controls, Meta, Source } from '@storybook/addon-docs/blocks'
import * as SliderStories from './Slider.stories'
import SliderExampleSource from './SliderExample.vue?raw'

<Meta of={SliderStories} />

# Slider

Ползунок для выбора одного значения, диапазона или нескольких непересекающихся диапазонов.

## Когда использовать

- `type="double"` — интервал между двумя значениями.
- `type="one-range"` — одно значение.
- `variant="block"` — компактное блочное отображение значений.
- `MultiSlider` — несколько интервалов на одной шкале; значения передаются парами.

## Базовый пример

<Canvas of={SliderStories.Double} />

<Controls of={SliderStories.Double} />

## Несколько диапазонов

<Canvas of={SliderStories.MultipleRanges} />

## Пример использования

<Source code={SliderExampleSource} language="vue" />
