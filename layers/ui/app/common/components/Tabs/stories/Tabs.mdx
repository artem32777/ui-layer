import { Canvas, Controls, Meta, Source } from '@storybook/addon-docs/blocks'
import * as TabsStories from './Tabs.stories'
import TabsExampleSource from './TabsExample.vue?raw'

<Meta of={TabsStories} />

# Tabs

Вкладки переключают между разделами Account и Password без перехода на другую страницу.

## Когда использовать

- `v-model` хранит значение активной вкладки.
- Используйте вкладки для равноправных разделов одного контекста.

## Базовый пример

<Canvas of={TabsStories.Account} />

<Controls of={TabsStories.Account} />

## Пример использования

<Source code={TabsExampleSource} language="vue" />
