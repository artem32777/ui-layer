import { Canvas, Controls, Meta, Source } from '@storybook/addon-docs/blocks'
import * as TabsStories from './Tabs.stories'
import TabsExampleSource from './TabsExample.vue?raw'

<Meta of={TabsStories} />

# Tabs
- Основан на <font color="blue">[`Tabs из Reka UI`](https://reka-ui.com/docs/components/tabs)</font>;
- Автоматически формирует именнованные слоты для контента из значений value массива заголовков;
- Массив заголовков нужно типизировать через `as const satisfies TabsItem[]`;

<Canvas of={TabsStories.Account} />

<Controls of={TabsStories.Account} />

## Пример использования

<Source code={TabsExampleSource} language="vue" />
