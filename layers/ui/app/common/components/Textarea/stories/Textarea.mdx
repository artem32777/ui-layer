import { Canvas, Controls, Meta, Source } from '@storybook/addon-docs/blocks'
import * as TextareaStories from './Textarea.stories'
import TextareaDocsExampleSource from './TextareaDocsExample.vue?raw'

<Meta of={TextareaStories} />

# Textarea

<Canvas of={TextareaStories.DocsExample} />

<Controls of={TextareaStories.DocsExample} />

## Пример использования

<Source code={TextareaDocsExampleSource} language="vue" />

## FormTextarea

Адаптер `Textarea` для `vee-validate`: значение, ошибка и лейбл по имени поля (`FormFieldProps`: `name`, `label`, `hint`).

Ошибка появляется после ухода с поля: `validateOnValueUpdate` выключен, валидация на blur. Чтобы скрыть ошибку, нужно исправить значение и снова уйти с поля.

<Canvas of={TextareaStories.FormDocsExample} />

### Состояние ошибки

<Canvas of={TextareaStories.Tests} />
