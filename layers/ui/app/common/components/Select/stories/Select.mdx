import { Canvas, Controls, Meta, Source } from '@storybook/addon-docs/blocks'
import * as SelectStories from './Select.stories'
import SelectDocsExampleSource from './SelectDocsExample.vue?raw'

<Meta of={SelectStories} />

# Select

- Основан на <font color="blue">[`Select из Reka UI`](https://reka-ui.com/docs/components/select)</font>;

<Canvas of={SelectStories.DocsExample} />

<Controls of={SelectStories.DocsExample} />

### Множественный выбор

<Canvas of={SelectStories.DocsMultiple} />

## Пример использования

<Source code={SelectDocsExampleSource} language="vue" />

## FormSelect

Адаптер `Select` для `vee-validate`: значение, ошибка и лейбл по имени поля (`FormFieldProps`: `name`, `label`).

Ошибка появляется после закрытия меню или ухода с поля: `validateOnValueUpdate` выключен, валидация на close/blur. Чтобы скрыть ошибку, нужно выбрать значение (или исправить и снова закрыть меню).

<Canvas of={SelectStories.FormDocsExample} />

### Состояние ошибки

<Canvas of={SelectStories.FormTests} />
